window.__BACKEND_URL__ = window.__BACKEND_URL__ || '';

// Set this to your deployed Express/Socket.IO backend URL, for example:
// window.__BACKEND_URL__ = 'https://your-backend.example.com';

console.info('Backend URL configured as:', window.__BACKEND_URL__ || 'same origin');
